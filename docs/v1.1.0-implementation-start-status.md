# v1.1.0 Implementation Start Status

Date: 2026-04-17
Scope: narrowed `v1.1.0` start checkpoint (`E + F1 + G1 + G2`)

## Current state

- `v1.1.0` implementation has started from the approved narrowed scope.
- The first integrated checkpoint is implemented and verified.
- The checkpoint establishes:
  - main-owned `HistoryStore` seam
  - durable monitoring event persistence
  - union queue model with manual tasks and recent completed items
  - Mission Control `F1` overlay
  - persisted timeline viewing

## What completed

- Added a main-process `HistoryStore` with a preferred SQLite adapter and JSON fallback under:
  - `src/main/history-store.ts`
- Wired workspace-local monitoring-history IPC/preload contracts for:
  - history status
  - session timeline queries
  - workspace feed queries
  - manual task CRUD / completion / reorder
- Persisted monitoring transitions from the main process into the history store.
- Kept manual task durability on the existing main-owned workspace JSON state path.
- Extended the sidebar queue from live attention only to:
  - live attention rows
  - manual task rows
  - recent completed rows
- Added the first `Mission Control` overlay trigger and UI surface.
- Extended terminal transition viewing so persisted timeline events survive renderer reloads.

## Verification

- `npm run build`
- `npm run test:unit`
- `npm run test:e2e:gate`
- `npx playwright test tests/e2e/v1-1-mission-control.spec.ts tests/e2e/v1-1-queue-manual-tasks.spec.ts tests/e2e/v1-1-timeline-persistence.spec.ts --project electron --timeout=120000 --workers=1`

## Remaining scope

- The current checkpoint does not open deferred `v1.1` items:
  - `F2`
  - `G3`
  - `H1`
  - `H2`
- Further hardening may still be needed around:
  - SQLite adapter stability across packaging/runtime environments
  - broader Mission Control ergonomics
  - release-grade verification for the narrowed `v1.1.0` lane

## Follow-up

- Continue `v1.1.0` work from this checkpoint rather than reopening planning.
- Keep all new work inside the narrowed `E + F1 + G1 + G2` boundary unless a new plan explicitly widens scope.
